# Core Discord Bot Dependencies - Railway Optimized
discord.py==2.3.2
python-dotenv==1.0.0

# AI Libraries - Essential only
openai==1.3.0
google-generativeai==0.3.0

# Basic utilities
aiohttp==3.9.0
requests==2.31.0
beautifulsoup4==4.12.2

# Document processing - lightweight
PyPDF2==3.0.1
python-docx==1.1.0

# Remove heavy dependencies that cause Railway timeouts:
# - chromadb (very heavy)
# - sentence-transformers (large models)
# - langchain (many dependencies)
# - moviepy (video processing)
# - pytesseract (OCR dependencies)

# These can be added later once the bot is stable
